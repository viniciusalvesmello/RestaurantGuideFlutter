import 'package:flutter_test/flutter_test.dart';

import 'package:RestaurantGuideFlutter/app/core/http_client/http_client_service.dart';

void main() {
  HttpClientService service;

  setUp(() {
//    service = HttpClientService();
  });
//
  group('HttpClientService Test', () {
//    test("First Test", () {
//      expect(service, isInstanceOf<HttpClientService>());
//    });
//
  });
}
